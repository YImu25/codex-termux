# 更新日志

本项目的脚本更新与重要修复记录在这里。运行 `cx update-script` 后，终端也会显示当前版本的更新提示。

## 2026.09.04.5 — 2026-09-04

### 修复

- 移除 `cx storage` 对 `/sdcard` 的重复 `--bind`，使用 proot-distro 已有的手机存储映射。
- Ubuntu 依赖新增系统版 `bubblewrap`，安装后验证 `bwrap` 命令可用。
- 修正终端更新提示中历史更新内容被错误显示为字面量 `\n` 的问题。

## 2026.09.04.4 — 2026-09-04

### 菜单优化

- 主菜单按“启动、配置、更新与信息、高级功能”重新分组。
- Codex 程序更新与菜单脚本更新相邻放置，版本信息紧随其后。
- 模型管理精简为查看、添加、删除、恢复和编辑五个入口。
- “删除本地模型”和“隐藏中转站模型”合并到“删除 / 隐藏模型”子菜单。
- 统一菜单中文名称、返回层级和选择范围提示。

## 2026.09.04.3 — 2026-09-04

### 修复与优化

- 修复旧版本已删除模型没有隐藏记录，仍会出现在“为已有中转站添加模型”远程列表中的问题。
- 新增“隐藏中转站返回的模型”，可直接从远程列表选择并隐藏历史残留模型。
- 新增“恢复隐藏模型”，避免误隐藏后只能手工修改文件。
- 重排模型管理菜单，将添加、删除、远程隐藏和恢复操作放在相邻位置。

## 2026.09.04.2 — 2026-09-04

### 修复

- 删除模型时按 Provider 与模型名称清理全部重复记录，不再仅按显示序号删除单行。
- 删除的模型会写入本地隐藏列表，切换入口和中转站自动获取结果都会统一过滤。
- 用户手动重新添加被删除的模型时，会自动解除隐藏并恢复使用。

## 2026.09.04.1 — 2026-09-04

### 新增

- 脚本安装或更新完成后显示版本号、发布日期和本次更新内容。
- 新增仓库更新日志，方便查看历史修改。

### 修复

- 修复基元律动 Responses 请求自动携带 `web_search` 时出现 `RESPONSES_FEATURE_NOT_SUPPORTED` 的问题。
- 选择、切换或更新已有基元律动配置时，自动设置 `web_search = "disabled"`。
- GitHub Actions 补充 `tomlkit` 测试依赖。
